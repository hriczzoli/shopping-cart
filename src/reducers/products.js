//PRODUCTS REDUCER
import defaultProducts from '../sampleProducts/productsDefault'

export default (state = defaultProducts, action) => {
    switch (action.type) {
        default:
            return state
    }
}